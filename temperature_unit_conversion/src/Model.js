const initModel = {
  leftValue: 0,
  leftUnit: 'Celsius',
  rightValue: 32,
  rightUnit: 'Fahrenheit',
  sourceLeft: true // determine if user enters initial value on the left or right
};

export default initModel;
